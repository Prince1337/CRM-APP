export interface PageEvent {
    pageIndex: number;    // Der Index der aktuell ausgewählten Seite (0-basiert)
    pageSize: number;     // Die Anzahl der Elemente pro Seite
    length: number;       // Die Gesamtanzahl der Elemente
  }
  